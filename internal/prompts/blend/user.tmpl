{{range $out := .OCROutputs}}<{{$out.TagName}}>
{{$out.Text}}
</{{$out.TagName}}>

{{end}}
Blend these OCR outputs into the best complete version, preserving all structure and content from any provider.
