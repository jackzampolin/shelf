type ToC {
    book: Book

    # Finder results
    toc_found: Boolean
    start_page: Int
    end_page: Int
    structure_summary: JSON

    # Status
    finder_complete: Boolean
    extract_complete: Boolean
    link_complete: Boolean

    # Relationships
    entries: [TocEntry]
}
