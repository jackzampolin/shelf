type Page {
    # Many-to-one: Page belongs to Book
    book: Book
    page_num: Int

    # OCR result (final markdown from OCR)
    ocr_markdown: String
    headings: JSON

    # Header/footer from Mistral OCR
    header: String
    footer: String

    # Status
    extract_complete: Boolean
    ocr_complete: Boolean
    # Explicit terminal exception. Quarantined pages are skipped by OCR and
    # remain distinguishable from successfully OCR'd pages.
    ocr_quarantined: Boolean
    ocr_quarantine_reason: String

    # Relationships
    # One-to-many: Page has many OcrResults
    ocr_results: [OcrResult]
}
